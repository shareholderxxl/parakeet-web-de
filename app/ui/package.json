{
  "name": "parakeet-preact-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node postbuild.mjs",
    "test": "node --test test/*.test.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.1.5"
  },
  "dependencies": {
    "quill": "^2.0.3"
  }
}
